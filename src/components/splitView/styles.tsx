import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  detailView: {
    flex: 2,
    overflow: "hidden",
  },
  masterView: { borderRightWidth: 1, flex: 1.5 },
  root: { flex: 1, flexDirection: "row" },
})
